echo "1. git init - Initialize a new Git repository
2. git clone <repo-url> - Clone a remote repository
3. git add <file> - Stage changes for commit
4. git commit -m 'message' - Commit staged changes
5. git push origin <branch> - Push changes to remote" > gitCommands.txt
